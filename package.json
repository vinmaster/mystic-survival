{
  "name": "mystic-survival",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm install && node index.js",
    "client": "cd client && npm install && parcel index.html"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "parcel": "^2.0.0"
  },
  "dependencies": {
    "ws": "^8.0.0",
    "pixi.js": "^7.2.4"
  }
}
